using UnityEngine;

public class RazerStats : MonoBehaviour
{
    public int atk;
    public int def;
    public int blc;
    public int stm;
    public int skl;
    public bool rotation;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
